## START BACKEND
1. npm init (this will initalize the backend files)
2. .gitignore this is generted to ignore the files that we dont want to push on github
3. env - this is the envirnoment packeges that we have that take locally instead of taking up from folder everytime
4.src folder 
5. public folder 